name: Swift

on: pull_request

jobs:
  build:

    runs-on: macOS-latest
    strategy:
      matrix: 
        destination: ['']

    steps:
    - uses: actions/checkout@v1
    - name: Build
      run: | 
        pod deintegrate
        pod install
